#include<stdio.h>

int main()
{
    int num1; 
    char ch;
    int num2;
    char str[10];

    scanf("%d %c %d %s", &num1, &ch, &num2, str);
    printf("%d %c %d %s\n", num1, ch, num2, str);
     // try with 10.5 a 
    return 0;
}